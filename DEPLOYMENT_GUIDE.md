# دليل نشر نظام الخزينة على Render 🚀

## البرنامج جاهز الآن ويعمل محلياً! ✅

التطبيق يعمل حالياً على: http://localhost:5000

## خطوات النشر على Render

### 1. رفع المشروع على GitHub

```bash
# الانتقال لمجلد المشروع
cd treasury_system

# تهيئة Git
git init

# إضافة جميع الملفات
git add .

# عمل commit
git commit -m "Initial commit - Treasury System"

# إنشاء مستودع جديد على GitHub
# اذهب إلى https://github.com/new وأنشئ مستودع جديد

# ربط المستودع المحلي بـ GitHub
git remote add origin https://github.com/YOUR_USERNAME/treasury-system.git

# رفع الكود
git push -u origin main
```

### 2. النشر على Render

1. **إنشاء حساب على Render**
   - اذهب إلى [render.com](https://render.com)
   - سجل حساب جديد أو سجل دخول

2. **إنشاء خدمة ويب جديدة**
   - اضغط على "New +" في لوحة التحكم
   - اختر "Web Service"
   - اربط حساب GitHub الخاص بك
   - اختر مستودع "treasury-system"

3. **إعدادات النشر**
   - **Name**: treasury-system (أو أي اسم تريده)
   - **Region**: اختر الأقرب لك
   - **Branch**: main
   - **Runtime**: Python 3
   - **Build Command**: 
     ```
     pip install -r requirements.txt
     ```
   - **Start Command**: 
     ```
     gunicorn app:app
     ```

4. **متغيرات البيئة**
   اضغط "Advanced" وأضف:
   - `PYTHON_VERSION` = `3.11`
   - `PORT` = `10000`

5. **إنشاء قاعدة البيانات PostgreSQL** (اختياري)
   - إذا أردت استخدام PostgreSQL بدلاً من SQLite:
   - اذهب لـ "New +" واختر "PostgreSQL"
   - أنشئ قاعدة بيانات جديدة
   - انسخ Database URL
   - أضفها كمتغير بيئة في Web Service:
     - `DATABASE_URL` = `postgresql://...`

6. **النشر**
   - اضغط "Create Web Service"
   - انتظر حتى يكتمل البناء والنشر
   - ستحصل على رابط مثل: `https://treasury-system.onrender.com`

### 3. للنشر المحلي (بديل)

إذا كنت تريد تشغيل البرنامج محلياً فقط:

```bash
# تثبيت المتطلبات
cd treasury_system
pip3 install --break-system-packages -r requirements.txt

# تشغيل التطبيق
python3 app.py

# أو باستخدام gunicorn
gunicorn app:app --bind 0.0.0.0:5000
```

ثم افتح المتصفح على: http://localhost:5000

## الميزات المتاحة 🎯

✅ **إدارة العملاء والموردين**
- إضافة وتعديل وحذف
- متابعة الأرصدة
- كشف حساب تفصيلي

✅ **نظام السندات المترابط**
- سندات قبض وصرف
- ترقيم تلقائي
- ربط بالعملاء والموردين والخزائن

✅ **الخزائن المتعددة**
- خزينة رئيسية وفرعية
- تحويلات بين الخزائن
- متابعة الأرصدة

✅ **الإدخال السريع**
- نموذج إدخال سريع في الصفحة الرئيسية
- اختصارات لوحة المفاتيح
- إكمال تلقائي للأسماء

✅ **التقارير الشاملة**
- تقارير يومية/شهرية
- كشف حساب العملاء والموردين
- تقرير الأرباح والخسائر
- رصيد سابق قبل تاريخ معين

✅ **لوحة تحكم ذكية**
- إحصائيات فورية
- آخر العمليات
- رسوم بيانية (قريباً)

## اختصارات لوحة المفاتيح ⌨️

- `Alt+N`: سند جديد
- `Alt+C`: البحث عن عميل
- `Alt+S`: البحث عن مورد
- `Alt+M`: التركيز على المبلغ
- `Alt+D`: التركيز على البيان
- `Enter`: حفظ السند

## ملاحظات مهمة ⚠️

1. **قاعدة البيانات**: 
   - محلياً: يستخدم SQLite (treasury.db)
   - على Render: يُفضل استخدام PostgreSQL

2. **الأمان**:
   - في الإنتاج، أضف نظام تسجيل دخول
   - استخدم HTTPS
   - قم بعمل نسخ احتياطية دورية

3. **الأداء**:
   - البرنامج يعمل بكفاءة مع آلاف السجلات
   - للأعداد الكبيرة جداً، استخدم PostgreSQL

## الدعم والمساعدة 🤝

إذا واجهت أي مشكلة:
1. تأكد من تثبيت جميع المتطلبات
2. تحقق من أن Python 3.8+ مثبت
3. راجع سجلات الأخطاء في app_full.log

## البرنامج جاهز للاستخدام! 🎉

يمكنك الآن:
- استخدامه محلياً مباشرة
- نشره على Render للوصول من أي مكان
- تخصيصه حسب احتياجاتك

بالتوفيق! 💪